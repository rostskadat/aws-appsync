import { FilterOnAttributesPipe } from './filter-on-attributes.pipe';

describe('FilterOnAttributesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnAttributesPipe();
    expect(pipe).toBeTruthy();
  });
});
